# v1.1.0 验证说明

本次新增私有仓库更新检查及窗口置顶。继续不生成本地 JSON 操作记录。新增版本比较、授权错误、下载包状态与 Token 格式测试。下方模板验证沿用 v1.0.1 的相同填充核心。

## 已完成

- GitHub Actions 原生 Windows Server 2022：15 项测试及严格 Clippy 检查通过。图钉测试直接创建系统窗口，验证置顶开关与位置、尺寸不变；不等同于完整界面人工验收。

- Rust Windows 单元测试与流程测试：15 项通过（包含置顶开关测试）。
- 相同的 15 项测试编译为 Windows x64 EXE，在独立 CrossOver / Wine 容器中运行通过，包含 Windows `ReplaceFileW` 覆盖路径。
- Windows 目标 `cargo clippy --all-targets -- -D warnings` 通过。
- Windows 原生界面程序成功编译；启动后进程保持运行，未出现先前 OpenGL 界面的启动错误。最终版本使用 Win32 控件，没有 OpenGL 依赖。
- Windows 命令行版本读取中文目录和补货表文件名、识别数据来源、创建备份、覆盖目标表均成功。
- 可执行文件检查确认为 PE32+ x86-64 Windows GUI；v1.1.0 正式发布包改由 Windows MSVC 构建。

## 五份实际模板的副本验证

使用用户提供的五份 XLSX 模板副本，按模板已有表格内容构造测试源报告。每站更新 7 张数据表。

| 模板站点 | 结果 | 完全保留的文件部件数 |
|---|---|---:|
| CA | 通过 | 50 |
| EU（FR 文件） | 通过 | 49 |
| JP | 通过 | 49 |
| UK | 通过 | 50 |
| US（追加 MX） | 通过 | 50 |

逐项验证：原件 SHA-256 未变；备份 SHA-256 等于更新前文件；其他工作表、外部链接及其余未修改部件的内容字节一致；更新工作表除数据区和尺寸声明外的 XML 结构一致。

专门验证 `1,185` 转为数值 1185，前导零标识 `001234` 保持文本，`23.71%`、`£2,052.55` 的内容不变。检查 US / MX 空两行且无重复 MX 表头，EU 国家顺序、各国表头和 Q 列国家标记。

新增测试覆盖：正式版本比较、同版本和旧版本判断、授权及限流错误、未上传完成的发布包、Token 换行注入拒绝，以及 Windows 置顶样式开关与窗口位置尺寸保持不变。

## 测试覆盖

普通数值及整数千位分隔符；固定列排除货币、百分比和标识列；旧尾部数据清除；源数据包含等号时按文本保存；US/MX 与 EU 拼接；源文件在识别后变化时拒绝覆盖；站点不匹配与报告列数变化时拒绝填充；备份开关；目标表锁文件；UTF-16 与日文编码；包含引号和逗号的文本解析。

## 尚未验证

- 原生 Windows 10 / 11 真机上的拖拽、文件选择、窗口显示和完整鼠标操作。当前环境为 macOS；CrossOver 原生窗口未能通过当前界面自动化接口完成可视化检查。不能将进程启动成功等同于界面全面验收。
- 用户实际下载的原始 CSV / TXT 文件尚未提供。构造输入测试无法覆盖所有导出格式与编码。
- Excel / WPS 打开后的实际公式重算结果。应用保留公式并请求打开时重算，不修复原模板既有错误，也没有将公式文本校验当作计算结果校验。

交付包不包含用户的业务模板、测试报告或补货数据。首次实际使用可保持默认勾选备份。
